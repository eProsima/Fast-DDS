// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file SimpleLargeTypeObject.cpp
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool fastddsgen.
 */

#ifdef _WIN32
// Remove linker warning LNK4221 on Visual Studio
namespace { char dummy; }
#endif

#include "SimpleLarge.h"
#include "SimpleLargeTypeObject.h"
#include <mutex>
#include <utility>
#include <sstream>
#include <fastdds/rtps/common/CdrSerialization.hpp>
#include <fastrtps/rtps/common/SerializedPayload.h>
#include <fastrtps/utils/md5.h>
#include <fastrtps/types/TypeObjectFactory.h>
#include <fastrtps/types/TypeNamesGenerator.h>
#include <fastrtps/types/AnnotationParameterValue.h>

using namespace eprosima::fastrtps::rtps;

void registerSimpleLargeTypes()
{
    static std::once_flag once_flag;
    std::call_once(once_flag, []()
            {
                TypeObjectFactory *factory = TypeObjectFactory::get_instance();
                factory->add_type_object("SimpleLarge_TypeIntrospectionExample", GetSimpleLarge_TypeIntrospectionExampleIdentifier(true),
                GetSimpleLarge_TypeIntrospectionExampleObject(true));
                factory->add_type_object("SimpleLarge_TypeIntrospectionExample", GetSimpleLarge_TypeIntrospectionExampleIdentifier(false),
                GetSimpleLarge_TypeIntrospectionExampleObject(false));

            });
}



const TypeIdentifier* GetSimpleLarge_TypeIntrospectionExampleIdentifier(bool complete)
{
    const TypeIdentifier * c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("SimpleLarge_TypeIntrospectionExample", complete);
    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
    {
        return c_identifier;
    }

    GetSimpleLarge_TypeIntrospectionExampleObject(complete); // Generated inside
    return TypeObjectFactory::get_instance()->get_type_identifier("SimpleLarge_TypeIntrospectionExample", complete);
}

const TypeObject* GetSimpleLarge_TypeIntrospectionExampleObject(bool complete)
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("SimpleLarge_TypeIntrospectionExample", complete);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }
    else if (complete)
    {
        return GetCompleteSimpleLarge_TypeIntrospectionExampleObject();
    }
    //else
    return GetMinimalSimpleLarge_TypeIntrospectionExampleObject();
}

const TypeObject* GetMinimalSimpleLarge_TypeIntrospectionExampleObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("SimpleLarge_TypeIntrospectionExample", false);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_MINIMAL);
    type_object->minimal()._d(TK_STRUCTURE);

    type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
    type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
    type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    MinimalStructMember mst_index;
    mst_index.common().member_id(memberId++);
    mst_index.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_index.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_index.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_index.common().member_flags().IS_OPTIONAL(false);
    mst_index.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_index.common().member_flags().IS_KEY(false);
    mst_index.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_index.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint32_t", false));


    MD5 index_hash("index");
    for(int i = 0; i < 4; ++i)
    {
        mst_index.detail().name_hash()[i] = index_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_index);

    MinimalStructMember mst_message;
    mst_message.common().member_id(memberId++);
    mst_message.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_message.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_message.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_message.common().member_flags().IS_OPTIONAL(false);
    mst_message.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_message.common().member_flags().IS_KEY(false);
    mst_message.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_message.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    MD5 message_hash("message");
    for(int i = 0; i < 4; ++i)
    {
        mst_message.detail().name_hash()[i] = message_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_message);

    MinimalStructMember mst_points;
    mst_points.common().member_id(memberId++);
    mst_points.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_points.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_points.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_points.common().member_flags().IS_OPTIONAL(false);
    mst_points.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_points.common().member_flags().IS_KEY(false);
    mst_points.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_points.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("int32_t", {3}, false));

    MD5 points_hash("points");
    for(int i = 0; i < 4; ++i)
    {
        mst_points.detail().name_hash()[i] = points_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_points);

    MinimalStructMember mst_second_message;
    mst_second_message.common().member_id(memberId++);
    mst_second_message.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_second_message.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_second_message.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_second_message.common().member_flags().IS_OPTIONAL(false);
    mst_second_message.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_second_message.common().member_flags().IS_KEY(false);
    mst_second_message.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_second_message.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    MD5 second_message_hash("second_message");
    for(int i = 0; i < 4; ++i)
    {
        mst_second_message.detail().name_hash()[i] = second_message_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_second_message);

    MinimalStructMember mst_some_values;
    mst_some_values.common().member_id(memberId++);
    mst_some_values.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_some_values.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_some_values.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_some_values.common().member_flags().IS_OPTIONAL(false);
    mst_some_values.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_some_values.common().member_flags().IS_KEY(false);
    mst_some_values.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_some_values.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("int16_t", 100, false));

    MD5 some_values_hash("some_values");
    for(int i = 0; i < 4; ++i)
    {
        mst_some_values.detail().name_hash()[i] = some_values_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_some_values);

    MinimalStructMember mst_is_it_not_true_that_true_is_not_true;
    mst_is_it_not_true_that_true_is_not_true.common().member_id(memberId++);
    mst_is_it_not_true_that_true_is_not_true.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_is_it_not_true_that_true_is_not_true.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_OPTIONAL(false);
    mst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_KEY(false);
    mst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_is_it_not_true_that_true_is_not_true.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));


    MD5 is_it_not_true_that_true_is_not_true_hash("is_it_not_true_that_true_is_not_true");
    for(int i = 0; i < 4; ++i)
    {
        mst_is_it_not_true_that_true_is_not_true.detail().name_hash()[i] = is_it_not_true_that_true_is_not_true_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_is_it_not_true_that_true_is_not_true);


    // Header
    // TODO Inheritance
    //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
    //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];
    TypeIdentifier identifier;
    identifier._d(EK_MINIMAL);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
        calculator.calculate_serialized_size(type_object->minimal().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("SimpleLarge_TypeIntrospectionExample", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("SimpleLarge_TypeIntrospectionExample", false);
}

const TypeObject* GetCompleteSimpleLarge_TypeIntrospectionExampleObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("SimpleLarge_TypeIntrospectionExample", true);
    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_COMPLETE);
    type_object->complete()._d(TK_STRUCTURE);

    type_object->complete().struct_type().struct_flags().IS_FINAL(false);
    type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->complete().struct_type().struct_flags().IS_NESTED(false);
    type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    CompleteStructMember cst_index;
    cst_index.common().member_id(memberId++);
    cst_index.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_index.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_index.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_index.common().member_flags().IS_OPTIONAL(false);
    cst_index.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_index.common().member_flags().IS_KEY(false);
    cst_index.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_index.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint32_t", false));


    cst_index.detail().name("index");

    type_object->complete().struct_type().member_seq().emplace_back(cst_index);

    CompleteStructMember cst_message;
    cst_message.common().member_id(memberId++);
    cst_message.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_message.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_message.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_message.common().member_flags().IS_OPTIONAL(false);
    cst_message.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_message.common().member_flags().IS_KEY(false);
    cst_message.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_message.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    cst_message.detail().name("message");

    type_object->complete().struct_type().member_seq().emplace_back(cst_message);

    CompleteStructMember cst_points;
    cst_points.common().member_id(memberId++);
    cst_points.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_points.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_points.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_points.common().member_flags().IS_OPTIONAL(false);
    cst_points.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_points.common().member_flags().IS_KEY(false);
    cst_points.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_points.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("int32_t", {3}, true));

    cst_points.detail().name("points");

    type_object->complete().struct_type().member_seq().emplace_back(cst_points);

    CompleteStructMember cst_second_message;
    cst_second_message.common().member_id(memberId++);
    cst_second_message.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_second_message.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_second_message.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_second_message.common().member_flags().IS_OPTIONAL(false);
    cst_second_message.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_second_message.common().member_flags().IS_KEY(false);
    cst_second_message.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_second_message.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    cst_second_message.detail().name("second_message");

    type_object->complete().struct_type().member_seq().emplace_back(cst_second_message);

    CompleteStructMember cst_some_values;
    cst_some_values.common().member_id(memberId++);
    cst_some_values.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_some_values.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_some_values.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_some_values.common().member_flags().IS_OPTIONAL(false);
    cst_some_values.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_some_values.common().member_flags().IS_KEY(false);
    cst_some_values.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_some_values.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("int16_t", 100, true));

    cst_some_values.detail().name("some_values");

    type_object->complete().struct_type().member_seq().emplace_back(cst_some_values);

    CompleteStructMember cst_is_it_not_true_that_true_is_not_true;
    cst_is_it_not_true_that_true_is_not_true.common().member_id(memberId++);
    cst_is_it_not_true_that_true_is_not_true.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_is_it_not_true_that_true_is_not_true.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_OPTIONAL(false);
    cst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_KEY(false);
    cst_is_it_not_true_that_true_is_not_true.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_is_it_not_true_that_true_is_not_true.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));


    cst_is_it_not_true_that_true_is_not_true.detail().name("is_it_not_true_that_true_is_not_true");

    type_object->complete().struct_type().member_seq().emplace_back(cst_is_it_not_true_that_true_is_not_true);


    // Header
    type_object->complete().struct_type().header().detail().type_name("SimpleLarge_TypeIntrospectionExample");
    // TODO inheritance
    TypeIdentifier identifier;
    identifier._d(EK_COMPLETE);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
        calculator.calculate_serialized_size(type_object->complete().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("SimpleLarge_TypeIntrospectionExample", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("SimpleLarge_TypeIntrospectionExample", true);
}
