// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file basic_inner_typesCdrAux.ipp
 * This source file contains some declarations of CDR related functions.
 *
 * This file was generated by the tool fastddsgen (version: 4.1.0).
 */

#ifndef FAST_DDS_GENERATED__BASIC_INNER_TYPESCDRAUX_IPP
#define FAST_DDS_GENERATED__BASIC_INNER_TYPESCDRAUX_IPP

#include "basic_inner_typesCdrAux.hpp"

#include <fastcdr/Cdr.h>
#include <fastcdr/CdrSizeCalculator.hpp>

#include <fastdds/dds/core/policy/QosPolicies.hpp>

#include <fastcdr/exceptions/BadParamException.h>
using namespace eprosima::fastcdr::exception;

namespace eprosima {
namespace fastcdr {


eProsima_user_DllExport bool is_InnerStructureHelper_cdr_plain(
        eprosima::fastdds::dds::DataRepresentationId_t data_representation)
{
    static_cast<void>(data_representation);
    return false;
}

template<>
eProsima_user_DllExport size_t calculate_serialized_size(
        eprosima::fastcdr::CdrSizeCalculator& calculator,
        const InnerStructureHelper& data,
        size_t& current_alignment)
{
    static_cast<void>(data);

    eprosima::fastcdr::EncodingAlgorithmFlag previous_encoding = calculator.get_encoding();
    size_t calculated_size {calculator.begin_calculate_type_serialized_size(
                                eprosima::fastcdr::CdrVersion::XCDRv2 == calculator.get_cdr_version() ?
                                eprosima::fastcdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                eprosima::fastcdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};


        calculated_size += calculator.calculate_member_serialized_size(eprosima::fastcdr::MemberId(0),
                data.field1(), current_alignment);

        calculated_size += calculator.calculate_member_serialized_size(eprosima::fastcdr::MemberId(1),
                data.field2(), current_alignment);


    calculated_size += calculator.end_calculate_type_serialized_size(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void serialize(
        eprosima::fastcdr::Cdr& scdr,
        const InnerStructureHelper& data)
{
    eprosima::fastcdr::Cdr::state current_state(scdr);
    scdr.begin_serialize_type(current_state,
            eprosima::fastcdr::CdrVersion::XCDRv2 == scdr.get_cdr_version() ?
            eprosima::fastcdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
            eprosima::fastcdr::EncodingAlgorithmFlag::PLAIN_CDR);

    scdr
        << eprosima::fastcdr::MemberId(0) << data.field1()
        << eprosima::fastcdr::MemberId(1) << data.field2()
;
    scdr.end_serialize_type(current_state);
}

template<>
eProsima_user_DllExport void serialize_array(
        eprosima::fastcdr::Cdr& scdr,
        const InnerStructureHelper* array_ptr,
        const std::size_t array_size)
{
    using namespace ::eprosima::fastdds::dds;
    DataRepresentationId_t data_representation = (scdr.get_cdr_version() == eprosima::fastcdr::CdrVersion::XCDRv1 ?
                DataRepresentationId_t::XCDR_DATA_REPRESENTATION : DataRepresentationId_t::XCDR2_DATA_REPRESENTATION);

    if (is_InnerStructureHelper_cdr_plain(data_representation) &&
            sizeof(InnerStructureHelper) == InnerStructureHelper_max_cdr_typesize)
    {
        // Serialize the first element
        // to ensure correct alignment
        scdr.serialize(
            array_ptr[0]);

        ++array_ptr;

        std::memcpy(
            scdr.get_current_position(),
            array_ptr,
            (array_size - 1) * sizeof(InnerStructureHelper));

        scdr.jump((array_size -1) * sizeof(InnerStructureHelper));
    }
    else
    {
        scdr.serialize_array(array_ptr, array_size);
    }
}

template<>
eProsima_user_DllExport void deserialize(
        eprosima::fastcdr::Cdr& cdr,
        InnerStructureHelper& data)
{
    cdr.deserialize_type(eprosima::fastcdr::CdrVersion::XCDRv2 == cdr.get_cdr_version() ?
            eprosima::fastcdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
            eprosima::fastcdr::EncodingAlgorithmFlag::PLAIN_CDR,
            [&data](eprosima::fastcdr::Cdr& dcdr, const eprosima::fastcdr::MemberId& mid) -> bool
            {
                bool ret_value = true;
                switch (mid.id)
                {
                                        case 0:
                                                dcdr >> data.field1();
                                            break;

                                        case 1:
                                                dcdr >> data.field2();
                                            break;

                    default:
                        ret_value = false;
                        break;
                }
                return ret_value;
            });
}

template<>
eProsima_user_DllExport void deserialize_array(
        eprosima::fastcdr::Cdr& scdr,
        InnerStructureHelper* array_ptr,
        const std::size_t array_size)
{
    using namespace ::eprosima::fastdds::dds;
        DataRepresentationId_t data_representation = (scdr.get_cdr_version() == eprosima::fastcdr::CdrVersion::XCDRv1 ?
                    DataRepresentationId_t::XCDR_DATA_REPRESENTATION : DataRepresentationId_t::XCDR2_DATA_REPRESENTATION);

    if (CdrVersion::XCDRv2 == scdr.get_cdr_version())
    {
        uint32_t dheader {0};
        scdr.deserialize(dheader);
        auto offset = scdr.get_current_position();

        if (is_InnerStructureHelper_cdr_plain(data_representation) &&
            sizeof(InnerStructureHelper) == InnerStructureHelper_max_cdr_typesize)
        {
            std::memcpy(
                reinterpret_cast<char*>(array_ptr),
                scdr.get_current_position(),
                (array_size) * sizeof(InnerStructureHelper));

            scdr.jump((array_size) * sizeof(InnerStructureHelper));
        }
        else
        {
            uint32_t count {0};
            while (scdr.get_current_position() - offset < dheader && count < array_size)
            {
                scdr.deserialize_array(&array_ptr[count], 1);
                ++count;
            }
        }

        if (scdr.get_current_position() - offset != dheader)
        {
            throw exception::BadParamException("Member size greater than size specified by DHEADER");
        }
    }
    else
    {
        if (is_InnerStructureHelper_cdr_plain(data_representation) &&
            sizeof(InnerStructureHelper) == InnerStructureHelper_max_cdr_typesize)
        {
            std::memcpy(
                reinterpret_cast<char*>(array_ptr),
                scdr.get_current_position(),
                (array_size) * sizeof(InnerStructureHelper));

            scdr.jump((array_size) * sizeof(InnerStructureHelper));
        }
        else
        {
            scdr.deserialize_array(array_ptr, array_size);
        }
    }
}

void serialize_key(
        eprosima::fastcdr::Cdr& scdr,
        const InnerStructureHelper& data)
{

    static_cast<void>(scdr);
    static_cast<void>(data);
                        scdr << data.field1();

                        scdr << data.field2();

}



eProsima_user_DllExport bool is_InnerEmptyStructureHelper_cdr_plain(
        eprosima::fastdds::dds::DataRepresentationId_t data_representation)
{
    static_cast<void>(data_representation);
    return false;
}

template<>
eProsima_user_DllExport size_t calculate_serialized_size(
        eprosima::fastcdr::CdrSizeCalculator& calculator,
        const InnerEmptyStructureHelper& data,
        size_t& current_alignment)
{
    static_cast<void>(data);

    eprosima::fastcdr::EncodingAlgorithmFlag previous_encoding = calculator.get_encoding();
    size_t calculated_size {calculator.begin_calculate_type_serialized_size(
                                eprosima::fastcdr::CdrVersion::XCDRv2 == calculator.get_cdr_version() ?
                                eprosima::fastcdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                eprosima::fastcdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};



    calculated_size += calculator.end_calculate_type_serialized_size(previous_encoding, current_alignment);

    return calculated_size;
}

template<>
eProsima_user_DllExport void serialize(
        eprosima::fastcdr::Cdr& scdr,
        const InnerEmptyStructureHelper& data)
{
    eprosima::fastcdr::Cdr::state current_state(scdr);
    scdr.begin_serialize_type(current_state,
            eprosima::fastcdr::CdrVersion::XCDRv2 == scdr.get_cdr_version() ?
            eprosima::fastcdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
            eprosima::fastcdr::EncodingAlgorithmFlag::PLAIN_CDR);

    static_cast<void>(data);

    scdr.end_serialize_type(current_state);
}

template<>
eProsima_user_DllExport void serialize_array(
        eprosima::fastcdr::Cdr& scdr,
        const InnerEmptyStructureHelper* array_ptr,
        const std::size_t array_size)
{
    using namespace ::eprosima::fastdds::dds;
    DataRepresentationId_t data_representation = (scdr.get_cdr_version() == eprosima::fastcdr::CdrVersion::XCDRv1 ?
                DataRepresentationId_t::XCDR_DATA_REPRESENTATION : DataRepresentationId_t::XCDR2_DATA_REPRESENTATION);

    if (is_InnerEmptyStructureHelper_cdr_plain(data_representation) &&
            sizeof(InnerEmptyStructureHelper) == InnerEmptyStructureHelper_max_cdr_typesize)
    {
        // Serialize the first element
        // to ensure correct alignment
        scdr.serialize(
            array_ptr[0]);

        ++array_ptr;

        std::memcpy(
            scdr.get_current_position(),
            array_ptr,
            (array_size - 1) * sizeof(InnerEmptyStructureHelper));

        scdr.jump((array_size -1) * sizeof(InnerEmptyStructureHelper));
    }
    else
    {
        scdr.serialize_array(array_ptr, array_size);
    }
}

template<>
eProsima_user_DllExport void deserialize(
        eprosima::fastcdr::Cdr& cdr,
        InnerEmptyStructureHelper& data)
{
    cdr.deserialize_type(eprosima::fastcdr::CdrVersion::XCDRv2 == cdr.get_cdr_version() ?
            eprosima::fastcdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
            eprosima::fastcdr::EncodingAlgorithmFlag::PLAIN_CDR,
            [&data](eprosima::fastcdr::Cdr& dcdr, const eprosima::fastcdr::MemberId& mid) -> bool
            {
                static_cast<void>(data);
                static_cast<void>(dcdr);
                static_cast<void>(mid);
                return false;
            });
}

template<>
eProsima_user_DllExport void deserialize_array(
        eprosima::fastcdr::Cdr& scdr,
        InnerEmptyStructureHelper* array_ptr,
        const std::size_t array_size)
{
    using namespace ::eprosima::fastdds::dds;
        DataRepresentationId_t data_representation = (scdr.get_cdr_version() == eprosima::fastcdr::CdrVersion::XCDRv1 ?
                    DataRepresentationId_t::XCDR_DATA_REPRESENTATION : DataRepresentationId_t::XCDR2_DATA_REPRESENTATION);

    if (CdrVersion::XCDRv2 == scdr.get_cdr_version())
    {
        uint32_t dheader {0};
        scdr.deserialize(dheader);
        auto offset = scdr.get_current_position();

        if (is_InnerEmptyStructureHelper_cdr_plain(data_representation) &&
            sizeof(InnerEmptyStructureHelper) == InnerEmptyStructureHelper_max_cdr_typesize)
        {
            std::memcpy(
                reinterpret_cast<char*>(array_ptr),
                scdr.get_current_position(),
                (array_size) * sizeof(InnerEmptyStructureHelper));

            scdr.jump((array_size) * sizeof(InnerEmptyStructureHelper));
        }
        else
        {
            uint32_t count {0};
            while (scdr.get_current_position() - offset < dheader && count < array_size)
            {
                scdr.deserialize_array(&array_ptr[count], 1);
                ++count;
            }
        }

        if (scdr.get_current_position() - offset != dheader)
        {
            throw exception::BadParamException("Member size greater than size specified by DHEADER");
        }
    }
    else
    {
        if (is_InnerEmptyStructureHelper_cdr_plain(data_representation) &&
            sizeof(InnerEmptyStructureHelper) == InnerEmptyStructureHelper_max_cdr_typesize)
        {
            std::memcpy(
                reinterpret_cast<char*>(array_ptr),
                scdr.get_current_position(),
                (array_size) * sizeof(InnerEmptyStructureHelper));

            scdr.jump((array_size) * sizeof(InnerEmptyStructureHelper));
        }
        else
        {
            scdr.deserialize_array(array_ptr, array_size);
        }
    }
}

void serialize_key(
        eprosima::fastcdr::Cdr& scdr,
        const InnerEmptyStructureHelper& data)
{

    static_cast<void>(scdr);
    static_cast<void>(data);
}


template<>
eProsima_user_DllExport size_t calculate_serialized_size(
        eprosima::fastcdr::CdrSizeCalculator& calculator,
        const InnerUnionHelper& data,
        size_t& current_alignment)
{
    static_cast<void>(data);

    eprosima::fastcdr::EncodingAlgorithmFlag previous_encoding = calculator.get_encoding();
    size_t calculated_size {calculator.begin_calculate_type_serialized_size(
                                eprosima::fastcdr::CdrVersion::XCDRv2 == calculator.get_cdr_version() ?
                                eprosima::fastcdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
                                eprosima::fastcdr::EncodingAlgorithmFlag::PLAIN_CDR,
                                current_alignment)};

    calculated_size += calculator.calculate_member_serialized_size(eprosima::fastcdr::MemberId(0), data._d(),
                    current_alignment);

    switch (data._d())
    {
                case 0:
                    calculated_size += calculator.calculate_member_serialized_size(eprosima::fastcdr::MemberId(1),
                                data.longValue(), current_alignment);
                    break;

                case 1:
                    calculated_size += calculator.calculate_member_serialized_size(eprosima::fastcdr::MemberId(2),
                                data.floatValue(), current_alignment);
                    break;

                default:
                    calculated_size += calculator.calculate_member_serialized_size(eprosima::fastcdr::MemberId(3),
                                data.shortValue(), current_alignment);
                    break;

    }

    calculated_size += calculator.end_calculate_type_serialized_size(previous_encoding, current_alignment);

    return calculated_size;
}


template<>
eProsima_user_DllExport void serialize(
        eprosima::fastcdr::Cdr& scdr,
        const InnerUnionHelper& data)
{
    eprosima::fastcdr::Cdr::state current_state(scdr);
    scdr.begin_serialize_type(current_state,
            eprosima::fastcdr::CdrVersion::XCDRv2 == scdr.get_cdr_version() ?
            eprosima::fastcdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
            eprosima::fastcdr::EncodingAlgorithmFlag::PLAIN_CDR);

    scdr << eprosima::fastcdr::MemberId(0) << data._d();

    switch (data._d())
    {
                case 0:
                    scdr << eprosima::fastcdr::MemberId(1) << data.longValue();
                    break;

                case 1:
                    scdr << eprosima::fastcdr::MemberId(2) << data.floatValue();
                    break;

                default:
                    scdr << eprosima::fastcdr::MemberId(3) << data.shortValue();
                    break;

    }

    scdr.end_serialize_type(current_state);
}

template<>
eProsima_user_DllExport void serialize_array(
        eprosima::fastcdr::Cdr& scdr,
        const InnerUnionHelper* array_ptr,
        const std::size_t array_size)
{
    using namespace ::eprosima::fastdds::dds;
    DataRepresentationId_t data_representation = (scdr.get_cdr_version() == eprosima::fastcdr::CdrVersion::XCDRv1 ?
                DataRepresentationId_t::XCDR_DATA_REPRESENTATION : DataRepresentationId_t::XCDR2_DATA_REPRESENTATION);

    if (is_InnerUnionHelper_cdr_plain(data_representation) &&
            sizeof(InnerUnionHelper) == InnerUnionHelper_max_cdr_typesize)
    {
        // Serialize the first element
        // to ensure correct alignment
        scdr.serialize(
            array_ptr[0]);

        ++array_ptr;

        std::memcpy(
            scdr.get_current_position(),
            array_ptr,
            (array_size - 1) * sizeof(InnerUnionHelper));

        scdr.jump((array_size -1) * sizeof(InnerUnionHelper));
    }
    else
    {
        scdr.serialize_array(array_ptr, array_size);
    }
}

template<>
eProsima_user_DllExport void deserialize(
        eprosima::fastcdr::Cdr& cdr,
        InnerUnionHelper& data)
{
    cdr.deserialize_type(eprosima::fastcdr::CdrVersion::XCDRv2 == cdr.get_cdr_version() ?
            eprosima::fastcdr::EncodingAlgorithmFlag::DELIMIT_CDR2 :
            eprosima::fastcdr::EncodingAlgorithmFlag::PLAIN_CDR,
            [&data](eprosima::fastcdr::Cdr& dcdr, const eprosima::fastcdr::MemberId& mid) -> bool
            {
                bool ret_value = true;
                if (0 == mid.id)
                {
                    int32_t discriminator;
                    dcdr >> discriminator;

                    switch (discriminator)
                    {
                                                case 0:
                                                    {
                                                        int32_t longValue_value{0};
                                                        data.longValue(std::move(longValue_value));
                                                        data._d(discriminator);
                                                        break;
                                                    }

                                                case 1:
                                                    {
                                                        float floatValue_value{0.0};
                                                        data.floatValue(std::move(floatValue_value));
                                                        data._d(discriminator);
                                                        break;
                                                    }

                                                default:
                                                    {
                                                        int16_t shortValue_value{0};
                                                        data.shortValue(std::move(shortValue_value));
                                                        data._d(discriminator);
                                                        break;
                                                    }

                    }
                }
                else
                {
                    switch (data._d())
                    {
                                                case 0:
                                                    dcdr >> data.longValue();
                                                    break;

                                                case 1:
                                                    dcdr >> data.floatValue();
                                                    break;

                                                default:
                                                    dcdr >> data.shortValue();
                                                    break;

                    }
                    ret_value = false;
                }
                return ret_value;
            });
}

template<>
eProsima_user_DllExport void deserialize_array(
        eprosima::fastcdr::Cdr& scdr,
        InnerUnionHelper* array_ptr,
        const std::size_t array_size)
{
    using namespace ::eprosima::fastdds::dds;
        DataRepresentationId_t data_representation = (scdr.get_cdr_version() == eprosima::fastcdr::CdrVersion::XCDRv1 ?
                    DataRepresentationId_t::XCDR_DATA_REPRESENTATION : DataRepresentationId_t::XCDR2_DATA_REPRESENTATION);

    if (CdrVersion::XCDRv2 == scdr.get_cdr_version())
    {
        uint32_t dheader {0};
        scdr.deserialize(dheader);
        auto offset = scdr.get_current_position();

        if (is_InnerUnionHelper_cdr_plain(data_representation) &&
            sizeof(InnerUnionHelper) == InnerUnionHelper_max_cdr_typesize)
        {
            std::memcpy(
                reinterpret_cast<char*>(array_ptr),
                scdr.get_current_position(),
                (array_size) * sizeof(InnerUnionHelper));

            scdr.jump((array_size) * sizeof(InnerUnionHelper));
        }
        else
        {
            uint32_t count {0};
            while (scdr.get_current_position() - offset < dheader && count < array_size)
            {
                scdr.deserialize_array(&array_ptr[count], 1);
                ++count;
            }
        }

        if (scdr.get_current_position() - offset != dheader)
        {
            throw exception::BadParamException("Member size greater than size specified by DHEADER");
        }
    }
    else
    {
        if (is_InnerUnionHelper_cdr_plain(data_representation) &&
            sizeof(InnerUnionHelper) == InnerUnionHelper_max_cdr_typesize)
        {
            std::memcpy(
                reinterpret_cast<char*>(array_ptr),
                scdr.get_current_position(),
                (array_size) * sizeof(InnerUnionHelper));

            scdr.jump((array_size) * sizeof(InnerUnionHelper));
        }
        else
        {
            scdr.deserialize_array(array_ptr, array_size);
        }
    }
}

template<>
eProsima_user_DllExport size_t calculate_serialized_size(
        eprosima::fastcdr::CdrSizeCalculator& calculator,
        const InnerBitsetHelper&,
        size_t& current_alignment)
{
    return calculator.calculate_serialized_size(std::bitset<33>{}, current_alignment);
}

template<>
eProsima_user_DllExport void serialize(
        eprosima::fastcdr::Cdr& scdr,
        const InnerBitsetHelper& data)
{
    std::bitset<33> bitset;

        bitset <<= 12;
        bitset |= (data.d & 0xFFF);

        bitset <<= 3;

        bitset <<= 10;
        bitset |= (data.c & 0x3FF);

        bitset <<= 4;

        bitset <<= 1;
        bitset |= (data.b & 0x1);

        bitset <<= 3;
        bitset |= (data.a & 0x7);


    scdr << bitset;
}

template<>
eProsima_user_DllExport void deserialize(
        eprosima::fastcdr::Cdr& dcdr,
        InnerBitsetHelper& data)
{
    std::bitset<33> bitset;
    dcdr >> bitset;

        data.a = static_cast<uint8_t>(bitset.to_ullong() & 0x7);
        bitset >>= 3;

        data.b = static_cast<bool>(bitset.to_ullong() & 0x1);
        bitset >>= 1;

        bitset >>= 4;

        data.c = static_cast<uint16_t>(bitset.to_ullong() & 0x3FF);
        bitset >>= 10;

        bitset >>= 3;

        data.d = static_cast<int16_t>(bitset.to_ullong() & 0xFFF);
        bitset >>= 12;

}


} // namespace fastcdr
} // namespace eprosima

#endif // FAST_DDS_GENERATED__BASIC_INNER_TYPESCDRAUX_IPP

